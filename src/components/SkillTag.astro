---
import type { Category } from '../config/colors';
import { categoryColors } from '../config/colors';

interface Props {
  name: string;
  category: Exclude<Category, 'all'>;
}

const { name, category } = Astro.props;
const color = categoryColors[category];
---

<span
  class="skill-tag px-2.5 md:px-4 py-1 md:py-1.5 rounded-full text-xs md:text-sm font-medium"
  style={`background-color: ${color}20; color: ${color}; border: 1.5px solid ${color}70;`}
>
  {name}
</span>

<style>
  .skill-tag {
    display: inline-block;
    white-space: nowrap;
  }
</style>

